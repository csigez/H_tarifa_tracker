[project]
name = "htariff-tracker"
version = "0.1.0"
description = "A django app to track electricity usage and costs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.1",
]
